# llm_chat_app/variables.tf
#
# This example is fully driven by config.yaml (loaded in main.tf via yamldecode):
# connection settings, databases, and roles + their grants all live there, so no
# input variables are needed here. The module at ../../ validates the decoded
# roles structure.
